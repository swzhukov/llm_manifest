# Оптимизация n8n на Beget

Source: DS | Date: 2026-06-04 | Messages: 2 | ID: c4e17633-543d-4998-a75d-801efda4f2a1

---

## user

Ты элитный мудрый промт-инженер, работающий в ит-интеграторах.
Задача настроить инструмент Qwen - Проекты. В нём я буду создавать различные инструменты на базе n8n, развёрнутой в конкретной среде. Т.е. мне нужно подготовить системный промт (инструкцию) и файлы, приаттаченные к этому проекту. Цель - чтобы как можно быстрее и эффективнее создавались воркфлоу с учётом всех особенностей конкретной среды (VPS, инсталляция n8n, используемые инсрументы, опции, доступы, токены, ключи, настройки и т.д.).
Контекст:
1. Во вложении - Экспорт чатов, где мы с тобой последовательно пытались решить одну задачу в конкретной среде исполнения Beget на базе n8n.
2. Файл n8n_newton_kb.md во вложении - это моё последнее описание среды/инструментов/инструкций и т.д.
3. Ниже текущая инструкция (системный промт) к этому проекту:
#ROLE: Elite n8n architect (Beget VPS, n8n v2.17.7, Newton).
#STRICT RULES:
1. NODES: readWriteFile(v1), if(v2), telegram(v1), httpRequest(v3), set(v3.3). NO fileOperations.
2. CREDS: NEVER output "credentials" block. User binds manually. Use {{ $credentials.telegram1.accessToken }}.
3. IF NODE: MUST include "combinator":"and" and "singleValue":true in operator, or UI hides it.
4. CODE: NO process.env. Use {{ $env.VAR }} or {{ $credentials.name.prop }}.
6. NETWORK: Flask wrapper is http://172.17.0.1:8080. NO localhost.
8. Язык ответов - русский.
9. Продумай правильную архитектуру такого решения, предложи, покритикуй, исправь. Повторяй цикл, пока остаются замечания. После этого создай решение: сделай, проверь/протестируй/покритикуй, исправь. Повторяй цикл, пока остаются замечания.
10. Всегда используй n8n_newton_kbmd максимально точно.
11. Всегда пытайся оптимизировать свою работу - переписывай системный скрипт и файл для контекста исполнения n8n_newton_kb.md.

Помоги мне сделать наилучшую настройку для оптимальной работы - хочется также избежать кучи повторений, потерь контекста, галлюцинаций. Ещё раз - решать в рамках этого проекта я буду сильно разные задачи по автоматизации. Общее между ними только: я буду делать только воркфлоу на конкретной инсталляции n8n в конкретной среда исполнения VPS Beget.

---

## user

1. То, что приведёт к оптимальному результату. Т.е. реши сам. Но учти конкретные особенности именно Qwen.
2. Так и не получится - там всего 1000 символов. Поэтому ты должен предложить сам как правильнее написать и системный промт и файлы.
3. Обязательно.

---

